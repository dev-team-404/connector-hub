# web

ConnectorHub Web — React SPA. Vite base 는 `/connector/` 이고, 사이트 껍데기는 AgentToolbox 가 발행하는 `@agent-factory/design-tokens`·`@agent-factory/site-shell` 을 버전 고정으로 쓴다. P3-3 에서 들어온다(선행: AgentToolbox #2941).
