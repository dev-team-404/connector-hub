"""외부 MCP 서버 접속 — SSRF 가드와 remote 클라이언트.

`core/connectors` 는 이 서비스의 **도메인**이고, 여기는 그 도메인이 바깥으로 나갈 때 쓰는
**능력**이다. 나눠 두는 이유는 경계가 다르기 때문이다 — 도메인 코드는 우리 DB 만 믿으면
되지만 이 아래 코드는 사용자가 넣은 주소로 나가므로 전부 적대적 입력을 가정한다.
"""
